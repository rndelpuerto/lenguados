# Function: formatMat2()

> **formatMat2**(`m`, `format`, `precision?`): `string`

Formats a 2x2 matrix as a string.

## Parameters

### m

[`ReadonlyMatrix2`](../../../../@lenguados/math2d/core/type-aliases/ReadonlyMatrix2.md)

Matrix to format

### format

Output format

`"json"` | `"flat"` | `"nested"`

### precision?

`number`

Number of decimal places

## Returns

`string`

Formatted string
